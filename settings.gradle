rootProject.name = "Flutter宿主项目-Android端"

include ':app'
setBinding(new Binding([gradle: this]))
evaluate(new File(
        settingsDir
        , 'module_flutter/.android/include_flutter.groovy'
))

//让Android Studio 去加载Flutter的模块。
include ':module_flutter'
